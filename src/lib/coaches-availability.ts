/**
 * Coaches catalog exists in the codebase but is not ready for public use yet.
 * Keep routes and components; flip this to `false` to restore the full UI.
 */
export const COACHES_COMING_SOON = true;
