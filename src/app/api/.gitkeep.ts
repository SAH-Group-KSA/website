/**
 * Reserved for server-only integration Route Handlers.
 * Secrets must never be imported into Client Components.
 *
 * Planned (when credentials exist):
 * - api/zoho/forms/*
 * - api/payments/moyasar/*
 * - api/payments/tamara/*
 * - api/newsletter/*
 */
export {};
