/**
 * SalesIQ widget — gate with `features.salesIq` + `env.zohoSalesIqWidgetCode()`.
 * Inject from locale layout only when both are set (no script until then).
 */
export function getSalesIqSnippet(): string | null {
  return null;
}
